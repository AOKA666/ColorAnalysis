{
  "name": "huekind-ai-color-analysis",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "generate:blog": "node scripts/generate-blog.mjs",
    "dev": "npm run generate:blog && vite",
    "build": "npm run generate:blog && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.0.16"
  }
}
